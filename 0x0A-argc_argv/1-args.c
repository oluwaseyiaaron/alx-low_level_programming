#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints the number of arguments passed into it.
 *
 * Return: return 0
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
